{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "generate-api": "openapi-generator-cli generate -i ./swagger.yaml -g typescript-axios -o ./src/api-client",
    "mock-api": "json-server --watch db.json --port 8080"
  },
  "dependencies": {
    "nuxt": "^3.15.2",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.15.3",
    "json-server": "^1.0.0-beta.3"
  }
}
